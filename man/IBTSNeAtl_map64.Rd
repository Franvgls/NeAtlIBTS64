% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/IBTSNeAtl_map64.R
\name{IBTSNeAtl_map64}
\alias{IBTSNeAtl_map64}
\title{Function IBTSNeAtl_map plots the map with all the NeAtl IBTS surveys}
\usage{
IBTSNeAtl_map64(
  xlims = NULL,
  ylims = NULL,
  leg = TRUE,
  legpos = "bottomright",
  cex.leg = 0.9,
  dens = 30,
  load = TRUE,
  ICESdiv = TRUE,
  ICESrect = FALSE,
  ICESlab = FALSE,
  ICESlabcex = 0.8,
  NS = FALSE,
  bathy = TRUE,
  bathy_col = "gray55",
  bathy_lwd = 0.4,
  bw = FALSE,
  bords = TRUE,
  axlab = 0.8,
  lwdl = 0.1,
  shpdir = system.file("shapes", package = "NeAtlIBTS64"),
  places = FALSE,
  minpop = 2e+05,
  newdev = TRUE,
  graf = FALSE,
  xpng = 1200,
  ypng = 800,
  ppng = 15
)
}
\arguments{
\item{xlims}{longitude limits as c(west, east). NULL (default) uses c(-18,3)
when NS=FALSE and c(-18,11) when NS=TRUE. User-supplied values always take precedence.}

\item{ylims}{latitude limits as c(south, north). NULL (default) uses c(36,60.5)
when NS=FALSE and c(36,62) when NS=TRUE. User-supplied values always take precedence.
Replaces the legacy nl/sl parameters.}

\item{leg}{if TRUE includes the legend (default TRUE)}

\item{legpos}{position of the legend (default "bottomright")}

\item{cex.leg}{size of the legend text (default 0.9)}

\item{dens}{density of shading lines for the survey polygons (default 30)}

\item{load}{if TRUE loads and plots the survey shapefiles (default TRUE)}

\item{ICESdiv}{if TRUE plots the ICES divisions (default TRUE)}

\item{ICESrect}{if TRUE plots ICES statistical rectangle grid (default FALSE)}

\item{ICESlab}{if TRUE plots ICES rectangle labels (default FALSE)}

\item{ICESlabcex}{size of ICES rectangle labels in cex (default 0.8)}

\item{NS}{if TRUE adds the North Sea ICES rectangle grid (default FALSE)}

\item{bathy}{if TRUE plots the isobaths (default TRUE)}

\item{bw}{if TRUE land in grey, if FALSE in burlywood3 (default FALSE)}

\item{bords}{if TRUE plots country borders (default TRUE)}

\item{axlab}{size of axis labels (default 0.8)}

\item{lwdl}{line width for shading (default 0.1)}

\item{shpdir}{path to the folder with the shapefiles}

\item{places}{if TRUE adds main cities (default FALSE)}

\item{minpop}{minimum population for cities to plot (default 200000)}

\item{newdev}{if TRUE opens a new graphics device with correct aspect ratio}

\item{graf}{if FALSE plots to screen; if a filename string, saves as PNG}

\item{xpng}{width of PNG in pixels (default 1200)}

\item{ypng}{height of PNG in pixels (default 800)}

\item{ppng}{pointsize for PNG (default 15)}
}
\description{
Produces a map from the shapefiles that define the IBTSNeAtl Surveys from
Scotland to Cadiz. Requires shapefiles in shpdir.
}
\details{
The default limits cover the full NeAtl IBTS area. The aspect ratio of the
plot window is calculated automatically from xlims and ylims using the
cosine correction at the mean latitude, so the map is always geographically
correct regardless of the limits used.
}
\examples{
\dontrun{
IBTSNeAtl_map(dens = 0, leg = TRUE, load = TRUE)
IBTSNeAtl_map(dens = 0, load = FALSE, ICESrect = TRUE)
}
}
