% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/MapIBTS64.R
\name{MapIBTS64}
\alias{MapIBTS64}
\title{Map species distribution from IBTS NeAtl surveys (R 4.4+)}
\usage{
MapIBTS64(
  data,
  esp = "HKE",
  year = NULL,
  heading = NULL,
  dato = 1,
  NS = TRUE,
  noutliers = 5,
  scaler = 0.35,
  sl = 35,
  nl = 61.5,
  metafile = FALSE,
  leg = TRUE,
  IBTSsurvs = NULL,
  extra_colors = c(`NS-IBTS` = "red", `IE-IAMS` = "darkgreen", NIGFS = "cyan4"),
  nfile = NA,
  pt = 18,
  png_w = NULL,
  png_h = NULL,
  png_res = NULL,
  bathy_col = "gray55",
  bathy_lwd = 0.4,
  newdev = TRUE,
  add = FALSE,
  Std.Surv = FALSE,
  Ord.Survs = TRUE,
  cexleg = 0.7
)
}
\arguments{
\item{data}{data.frame. Output of \code{IBTSGetSpeciesCPUE()}, or any
data.frame with columns:
Survey_Code, Longitude, Latitude, Survey_Year, Haul_Code, Vessel_Code,
Species_Code, Common_Name, Length_Split, Group1, Group2, Total.}

\item{esp}{Character. Species code to plot (e.g. \code{"HKE"}).
Use \code{"Stations"} to plot station positions only.}

\item{year}{Integer. Year to select. If NULL uses all rows in \code{data}.}

\item{heading}{Character. Species description for the legend title
(e.g. \code{"Hake <20 cm"}). If NULL derived from Common_Name.}

\item{dato}{Integer. Which column to plot: 1 = Group1 (small / pre-recruit),
2 = Group2 (large / post-recruit), 3 = Total. Default 1.}

\item{NS}{Logical. If TRUE (default) includes North Sea (wider xlim).}

\item{noutliers}{Integer. Number of top-value hauls to treat as outliers
(drawn as open circles with a "> N" label). Default 5.}

\item{scaler}{Numeric. Bubble-size divisor. Increase to shrink all bubbles.
Default 0.35 (same as legacy MapIBTS).}

\item{sl}{Numeric. Southern latitude limit (degrees N). Default 35.}

\item{nl}{Numeric. Northern latitude limit (degrees N). Default 61.5.}

\item{metafile}{Logical. If TRUE saves output to a PNG file. Default FALSE.}

\item{leg}{Logical. Draw the legend. Default TRUE.}

\item{IBTSsurvs}{data.frame. Survey colour table (columns: survey, color, ...).
If NULL tries the package dataset \code{IBTSsurvs}.}

\item{extra_colors}{Named character vector of colours for surveys not in
\code{IBTSsurvs} (e.g. NS-IBTS, IE-IAMS, NIGFS Q1).
Default: \code{c("NS-IBTS"="red","IE-IAMS"="darkgreen","NIGFS"="cyan4")}.}

\item{nfile}{Character. Output PNG filename without extension. Auto-generated
from \code{esp} + \code{dato} + \code{NS} if NA.}

\item{pt}{Numeric. PNG pointsize. Default 18.}

\item{png_w}{Integer. PNG width in pixels. Default 600 (NS=FALSE) or 800 (NS=TRUE).}

\item{png_h}{Integer. PNG height in pixels. Default 1200 (NS=FALSE) or 1100 (NS=TRUE).}

\item{png_res}{Integer. PNG resolution in dpi. Default NULL (uses R's default ~72 dpi).
Set to 150 or 300 for high-resolution output.}

\item{bathy_col}{Colour for isobath lines passed to IBTSNeAtl_map64().
Default "gray55" (visible but light over lightblue1 background).}

\item{bathy_lwd}{Line width for isobath lines. Default 0.4.}

\item{newdev}{Logical. Open a new graphics window. Default TRUE.
Set to FALSE when drawing into an already-open device (PNG, mfrow, etc.).}

\item{add}{Logical. If TRUE skips drawing the base map and only adds points
and legend to the current device. Default FALSE.}

\item{Std.Surv}{Logical. If TRUE standardises each survey to its own maximum
(use with care -- breaks cross-survey comparability). Default FALSE.}

\item{Ord.Survs}{Logical. If TRUE (default) orders legend north to south.}

\item{cexleg}{Numeric. Character expansion for legend text. Default 0.7.}
}
\value{
Invisibly, a list with elements:
\code{data} (the filtered/annotated data.frame),
\code{m} (the non-outlier maximum used for scaling),
\code{ml} (the rounded scale maximum),
\code{col_map} (named colour vector by survey).
}
\description{
Phase 2 of the species distribution mapping pipeline. Plots bubble maps of
species CPUE (numbers per hour) from all IBTS NeAtl surveys on the
\code{IBTSNeAtl_map64()} base map.  Outliers (the top N values) are shown
with open circles so that the rest of the data uses a consistent size scale.
}
\details{
Replaces the legacy \code{MapIBTS()} function from \code{mapIBTS_csv_IBTSWG2024.R},
removing all dependency on maptools, rgdal, sp and the hardcoded colour vector.
}
\examples{
\dontrun{
# ---- Typical single-species map ----------------------------------------
devtools::load_all("d:/FVG/GitHubRs/NeAtlIBTS64")
ibts25  <- IBTSSurveySummary(2025)
cpue25  <- IBTSGetSpeciesCPUE(2025, species = read.csv("SpeciesCodes.csv"),
                               HHdata = ibts25)

# Pre-recruit hake (Group1 = <20 cm)
MapIBTS64(cpue25, esp = "HKE", year = 2025,
          heading = "Hake <20 cm", dato = 1, NS = FALSE)

# Post-recruit hake (Group2 = >=20 cm)
MapIBTS64(cpue25, esp = "HKE", year = 2025,
          heading = "Hake 20+ cm", dato = 2, NS = FALSE)

# ---- PNG with two panels (Q1 left, Q3+Q4 right) ----------------------
png("HKE_2025.png", width = 1600, height = 2000, pointsize = 18)
par(mfrow = c(1, 2))
MapIBTS64(cpue25, esp = "HKE", year = 2025, heading = "Hake <20cm Q1",
          dato = 1, NS = FALSE, newdev = FALSE)
MapIBTS64(cpue25, esp = "HKE", year = 2025, heading = "Hake <20cm Q3+Q4",
          dato = 1, NS = FALSE, newdev = FALSE)
dev.off()

# ---- Station positions only -------------------------------------------
MapIBTS64(cpue25, esp = "Stations", year = 2025)
}

}
